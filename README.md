# Neovim configuration

You only need to clone the repository to the nvim's folder and tweak some settings according to your system.

```sh
git clone --recursive https://github.com/aleDsz/vimfiles.git ~/.config/nvim
```
